    <!-- Navbar Start -->
<div class="container-fluid">
    <div class="row border-top px-xl-5">
        <div class="col-lg-2 d-none d-lg-block">
            <nav class="collapse position-absolute navbar navbar-vertical navbar-light align-items-start p-0 border border-top-0 border-bottom-0 bg-light" id="navbar-vertical" style="width: calc(100% - 30px); z-index: 1;">
            </nav>
        </div>
        <div class="col-lg-9">
            <nav class="navbar navbar-expand-lg bg-light navbar-light py-3 py-lg-0 px-0">
                <a href="@Url.Action("Welcome","Home","Home")" class="text-decoration-none d-block d-lg-none">
                    <h1 class="m-0 display-5 font-weight-semi-bold"><span class="text-primary font-weight-bold border px-3 mr-1">E</span>Tienda MVC</h1>
                </a>
                <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                    <div class="navbar-nav mr-auto py-0">

                        @{
                            if (User.IsInRole("Administrador"))
                            {
                                <a href="@Url.Action("Index", "Cliente", "Cliente")" class="nav-item nav-link">Clientes </a>
                                <a href="@Url.Action("Index", "Producto", "Producto")" class="nav-item nav-link"> Productos</a>

                            }
                            else if (User.IsInRole("Proveedor"))
                            {
                                <a href="@Url.Action("Index","Proveedor",  "Proveedor")" class="nav-item nav-link"> Proveedor </a>
                            }
                            else if (User.IsInRole("Cliente"))
                            {
                                <a href="@Url.Action("Index", "Pedido", "Pedido")" class="nav-item nav-link"> Pedidos</a>
                                <a href="@Url.Action("Index", "PedidosItem", "PedidosItem")" class="nav-item nav-link">  <i class="fas fa-shopping-cart text-primary"></i> Carrito </a>
                                <a href="@Url.Action("Index","Producto",  "Producto")" class="nav-item nav-link"> Productos </a>
                                <a href="@Url.Action( "VerAllPedidos","PedidosItem", "PedidosItem")" class="nav-item nav-link">Ver Pedidos </a>
                            }
                        }
                    </div>
                    <div class="navbar-nav ml-auto py-0">
                        @Html.Partial("_LoginPartial")
                    </div>
                </div>
            </nav>
        </div>
    </div>
</div>
<!-- Navbar End -->